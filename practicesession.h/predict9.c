#include<stdio.h>
int main()
{
    int a;
    if(3 + 2 % 5)
    printf("this works");
    if(a=10)
    printf("even this works");
    if(-5)
    printf("surprisingly even this works");
    return 0;
}