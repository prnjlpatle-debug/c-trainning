#include<stdio.h>
#include<limits.h>
int main()
{  
   int a[5]={1,-2,3,-4,5};
   int maxSum=0;
   for (int i = 0; i<5; i++)
    curSum+=a[]
        if (curSum> maxSum)
        {
            maxSum = curSum;
        }
        } 
    printf("%d", maxSum);
   }
